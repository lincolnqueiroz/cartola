import { Box, Paper, Typography, Toolbar, AppBar } from "@mui/material";


function Header(){
    return(
        <header>
            <Grid>
                <AppBar></AppBar>
            </Grid>
        </header>
    )
}

export default Header;
